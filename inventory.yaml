groups:
  - name: linux
    nodes:
      - 1.2.3.4 .  # REPLACE ME
    config:
      transport: ssh
      ssh:
        host-key-check: false
        user: centos
        private-key: /path/to/key   # REPLACE ME
  - name: windows
    nodes:
      - 5.6.7.8 .   # REPLACE ME
    config:
      transport: winrm
      winrm:
        user: administrator
        password: REPLACEME  # REPLACE ME
        ssl: false
